%table
  %thead
    %tr
      %th Title
      %th Content
      %th
      %th
      %th

  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= post.content
        %td= link_to 'Show', admins_post_path(post)

%br

= link_to 'New Post', new_admins_post_path
