
.mb-3= link_to '新規登録', new_admins_post_path, class: 'btn btn-primary'

%table.table
  %thead
    %tr
      %th タイトル
      %th 内容
      %th 作成日時
      %th 更新日時

  %tbody
    - @posts.each do |post|
      %tr
        %td
          = link_to edit_admins_post_path(post) do
            = truncate(post.title)
        %td= truncate(post.content)
        %td= l(post.created_at)
        %td= l(post.updated_at)
